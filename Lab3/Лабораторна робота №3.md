# **Тема: Маніпулювання даними SQL (OLTP)**
---
## Цілі
---
- Написати запити `SELECT` для отримання даних (включаючи фільтрацію за допомогою `WHERE` та вибір певних стовпців)
- Практикувати використання операторів `INSERT` для додавання нових рядків до таблиць
- Практикувати використання оператора `UPDATE` для зміни існуючих рядків (використовуючи `SET` та `WHERE`)
- Практикувати використання операторів `DELETE` для безпечного видалення рядків (за допомогою `WHERE`)
- Вивчити основні операції маніпулювання даними (DML) у PostgreSQL та спостерігати за їхнім впливом

## Таблиці
---
- `users` - користувач
- `developers` - розробники
- `employers` - роботодавці
- `projects` - проєкти

## SQL-скрипт бази даних
---
```sql
DROP TABLE IF EXISTS projects;
DROP TABLE IF EXISTS developers;
DROP TABLE IF EXISTS employers;
DROP TABLE IF EXISTS users;

CREATE TABLE users (
	user_id SERIAL PRIMARY KEY,
	user_name VARCHAR(30) NOT NULL,
	user_surname VARCHAR(50) NOT NULL,
	user_country VARCHAR(100) NOT NULL,
	user_email VARCHAR(100) NOT NULL UNIQUE,
	user_phone VARCHAR(18) NOT NULL
);

CREATE TABLE developers (
	dev_id INT PRIMARY KEY REFERENCES users(user_id) ON DELETE CASCADE,
	is_searching_job BOOLEAN NOT NULL DEFAULT TRUE,
	dev_links TEXT
);

CREATE TABLE employers (
	employer_id INT PRIMARY KEY REFERENCES users(user_id) ON DELETE CASCADE,
	employer_company VARCHAR(150) NOT NULL,
	employer_position VARCHAR(100) NOT NULL
);

CREATE TABLE projects (
	project_id SERIAL PRIMARY KEY,
	dev_id INT NOT NULL REFERENCES developers(dev_id) ON DELETE CASCADE,
	project_name VARCHAR(150) NOT NULL,
	project_description TEXT NOT NULL,
	project_url VARCHAR(255) NOT NULL,
	project_category VARCHAR(50) NOT NULL,
	project_active BOOLEAN NOT NULL DEFAULT TRUE
);
```


## Хід роботи
---
# `SELECT`
---
Код SQL
```sql
SELECT user_name, user_surname, user_phone
FROM users
WHERE user_country = 'Ukraine'
```

Очікуваний результат:
Список з 3 українських користувачів, що включає імʼя, призвіще та номер телефону

## Результат
![[Screenshot 2026-05-18 at 20.52.50.png|697]]

# `JOIN`
---
Код SQL
```sql
SELECT project_name, project_category, project_url 
FROM projects 
WHERE project_category = '#frontend' AND project_active = TRUE;
```

Очікуваний результат:
Таблиця з двома проєктами

## Результат
![[Pasted image 20260518205602.png]]

# `INSERT`
---
Код SQL
```sql
INSERT INTO users (user_name, user_surname, user_country, user_email, user_phone) VALUES
('Владислав', 'Максименко', 'Ukraine', 'maksymenkovladislav@gmail.com', '+380978876543');

INSERT INTO developers (dev_id, is_searching_job, dev_links) VALUES
(6, TRUE, 'https://www.github.com/vladyslav');

INSERT INTO projects (dev_id, project_name, project_description, project_url, project_category, project_active) VALUES
(6, 'Recipe Book API', 'REST API для книги рецептів', 'https://www.recipesapi.com', '#backend', TRUE);
```

Очікуваний результат:
Користувач Владислав зʼявиться у таблицях `users`, `developers`, `projects`

## Результат
![[Screenshot 2026-05-18 at 21.05.34.png]]
![[Screenshot 2026-05-18 at 21.06.11.png]]

## `UPDATE`
---
Код SQL
```sql
UPDATE developers
SET is_seacrhing_job = FALSE
WHERE dev_id = 4;
```

Очікуваний результат:
Статус пошуку роботи користувача зміниться з `TRUE` на `FALSE` 

## Результат
![[Screenshot 2026-05-18 at 21.09.24.png]]

## `DELETE` 
---
## Результат до
![[Screenshot 2026-05-18 at 21.14.46.png]]

Код SQL
```sql
DELETE FROM projects
WHERE dev_id = 1 AND project_name = 'E-Commerce App';
```

Очікуваний результат:
Проєкт E-Commerce App буде назавжди видалено з бази

## Результат після
![[Screenshot 2026-05-18 at 21.17.13.png]]

## `ON DELETE CASCADE`
---
## Результат до
![[Pasted image 20260518211951.png]]

Код SQL
```sql
DELETE FROM users WHERE user_id = 3;
```

Очікуваний результат:
Користувача видалено

## Результат після
![[Screenshot 2026-05-18 at 21.22.17.png]]

## Висновки
---
У ході виконання лабораторної роботи було виконано основні DML-операції. Було додано нові дані до таблиці, видалено абсолютно всі дані користувача з усіх таблиці, видалено дані про проєкт користувача, оновлено дані в таблиці та ін. За результатами запуску кожного з скриптів помилок не виникло, що свідчить про коректну роботу скриптів
